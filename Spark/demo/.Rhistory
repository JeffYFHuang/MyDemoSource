.libPaths(c(file.path(Sys.getenv("SPARK_HOME"), "R", "lib"), .libPaths()))
library(SparkR)
library(SparkR)
sc <- sparkR.init(master = "local")
sqlContext <- sparkRSQL.init(sc)
exercise.df <- data.frame(name=c("John", "Smith", "Sarah"), age=c(19, 23, 18)) 
spark.df <- createDataFrame(sqlContext, exercise.df)
head(spark.df)
printSchema(spark.df)
plot(spark.f)
plot(spark.df)
spark.df
head(spark.df)
q()
library(SparkR)
.libPaths(c(file.path(Sys.getenv("SPARK_HOME"), "R", "lib"), .libPaths()))
library(SparkR)
sc <- sparkR.init(master = "local")
sqlContext <- sparkRSQL.init(sc)
exercise.df <- data.frame(name=c("John", "Smith", "Sarah"), age=c(19, 23, 18)) 
spark.df <- createDataFrame(sqlContext, exercise.df)
head(spark.df)
sqlContext
newiris = iris;names(newiris) = c("Sepal_Length","Sepal_Width","Petal_Length","Petal_Width","Species") 
newiris
df = createDataFrame(sqlContext, newiris) 
head(df, 5) showDF(df, 5) 
head(df, 5);showDF(df, 5) 
printSchema(df) 
 model = glm(Sepal_Length ~., data = df, family = "gaussian");summary(model)
q()
